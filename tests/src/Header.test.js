import React from 'react';
import { render } from '@testing-library/react';
import Header from '../../client/src/Header';
import Home from '../../client/src/Header';
import Recipe from '../../client/src/Header';
import Price from '../../client/src/Header';
import Diet from '../../client/src/Header';
import Prof from '../../client/src/Header';

test('Header has all sections', () => {
    // const result = Header();
    // expect(result).toBe(<header className="header" style={{"background": "lemonchiffon", "fontFamily": "cursive"}}><nav><ul className="right"><div className="left" style={{"fontFamily": "cursive", "fontSize": "30px"}}>WhatToCook</div><Home /><Recipe /><Price /><Diet /><Prof /></ul></nav><ul style={{"color": "lemonchiffon", "fontSize": "15px"}}>Space</ul></header>);
});